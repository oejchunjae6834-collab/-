## 🛠️ 디적디적 홈페이지 기술 요구사항 고도화 (TRD)

### **1. 세션별·가족 단위 출석 체크(RSVP) 시스템**

기존의 단순 여부 확인을 넘어, 한 계정(부모)이 여러 구성원(자녀)의 세션별 참여를 관리하는 **다차원 데이터 구조**로 설계해야 합니다.

#### **A. 데이터베이스 스키마(Schema) 최적화**
*   **`Sessions` 테이블:** 각 모임(Event)에 종속된 하위 세션 정보를 관리합니다.[cite: 9, 10, 11]
    *   `session_id`, `event_id`, `session_name` (수학이랑 놀자(13:30 ~ 15:00)/공동체 놀이(15:00 ~ 15:30)/아이들 모여라(15:40 ~ 17:30)/어른 공부 모임(18:40 ~ 20:10)), `start_time`, `end_time`[cite: 5, 6, 7, 11]
*   **`Family_Members` 테이블:** 회원(부모) 계정에 연결된 자녀 정보를 저장합니다.[cite: 2, 8]
    *   `member_id`, `parent_user_id`, `name`, `type` (자녀/부모)[cite: 8]
*   **`Attendances` 테이블:** 세션별-인원별 출석 상태를 기록합니다.
    *   `attendance_id`, `session_id`, `member_id`, `status` (참석/불참/미정)

#### **B. UI/UX 구현 로직: "Family-Session Grid"**
*   **매트릭스 인터페이스:** 가로축은 **시간대별 세션**, 세로축은 **가족 구성원**으로 구성된 그리드형 체크박스 리스트를 제공합니다.
*   **자동화 기능:** '전체 세션 참여' 버튼을 통해 한 번에 모든 체크박스를 활성화하는 기능을 포함합니다.
*   **실시간 가용성:** 특정 세션(예: 마하정식 저녁 식사)의 경우 준비를 위해 총 인원수가 실시간으로 합산되어 관리자와 회원에게 노출되어야 합니다.[cite: 4, 5]

---

### **2. 관리자(Admin) 백오피스 및 CMS 확장**

관리자가 코드 수정 없이 운영 전반을 통제할 수 있는 **중앙 집중형 관리 도구**를 구축합니다.

#### **A. 전 메뉴 통합 콘텐츠 관리 (Full-CRUD)**
*   **Unified Dashboard:**모든 게시판의 글을 한곳에서 모니터링하고 수정/삭제할 수 있는 관리자 전용 리스트 뷰를 구현합니다.
*   **권한 제어:** 특정 게시글의 열람 권한을 '전체 회원' 또는 '운영진'으로 즉시 변경할 수 있는 토글 기능을 포함합니다.[cite: 8]

#### **B. 활동 내용(Portfolio) 갤러리 에디터**
*   **드래그 앤 드롭(Drag & Drop) 정렬:** 비회원에게 보여줄 활동 사진이나 영상의 순서를 관리자 페이지에서 마우스로 끌어서 변경할 수 있는 정렬 기능을 도입합니다.[cite: 5, 6, 7]
*   **태그 시스템:** '수학 활동', 'AI 프로젝트', '여름/겨울 캠프' 등 카테고리 태그를 관리자가 직접 생성하고 게시물에 할당하여 비회원이 쉽게 필터링할 수 있도록 합니다.[cite: 2, 4, 10, 11]

#### **C. 회원 가입 및 승인 워크플로우**
*   **가족 정보 기록:** 신규 가입 시 부모 정보 외에도 자녀의 이름과 나이를 기록하게 하여, 승인 후 즉시 출석 체크 명단에 반영되도록 합니다.[cite: 8]
*   **관리자 승인 대기열:** 가입 신청 시 `is_approved` 상태를 `false`로 설정하고, 관리자가 대시보드에서 승인 버튼을 누르는 즉시 정회원 권한과 회의록 접근권을 부여합니다.

---

### **3. 시스템 메시지 및 알림 (추가 제안)**
*   **출석 마감 알림:** 정기 모임 전(예: 4월 19일 모임 전 24시간)까지 출석 체크를 하지 않은 회원에게 대시보드 내 알림 노출.[cite: 9]
*   **AI 검색 최적화:** 관리자가 새로 올린 회의록 데이터가 즉시 AI 검색(RAG) 엔진의 지식 베이스에 포함되도록 자동 인덱싱 파이프라인을 연결합니다.

이 기술 사양을 클로드 코드에게 전달하면, **"세션별-가족 단위의 관계형 데이터 구조를 설계하고 관리자용 포트폴리오 에디터를 구현해달라"**는 핵심 지시가 명확하게 전달될 것입니다. 조율이 더 필요한 부분이 있으신가요?
